export const OPERATIONAL_ACC = {
  // address: process.env.BROKER_ACC_ADDRESS!,
  // publicKey: process.env.BROKER_ACC_PUB_KEY!,
  seed: process.env.BROKER_ACC_SEED!,
};

export const COLLECTION_NAMES = {
  COLLECTIBLES: "collectibles",
  CREATOR_PROFILES: "creator-profiles",
};
